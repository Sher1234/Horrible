<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <View
        android:id="@+id/view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/white" />

    <include layout="@layout/layout_b" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/primaryDark"
        android:focusable="true"
        app:headerLayout="@layout/navigation_header"
        app:itemBackground="@drawable/navigation_item_background_dark"
        app:itemIconTint="@color/navigation_item_dark"
        app:itemTextAppearance="@style/TextAppearance.MaterialComponents.Body2"
        app:itemTextColor="@color/navigation_item_dark"
        app:menu="@menu/navigation" />

</androidx.drawerlayout.widget.DrawerLayout>