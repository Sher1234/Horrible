<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="info.horriblesubs.sher">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:fullBackupContent="@xml/backup_descriptor"
        android:roundIcon="@mipmap/ic_launcher"
        tools:ignore="GoogleAppIndexingWarning"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:name=".AppMe">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/ad_mob_app_id" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification_new" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorText" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/notification_channel" />

        <service
            android:name=".service.MessagingService"
            tools:ignore="ExportedService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity android:name=".ui.b.Explore" android:configChanges="uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity android:name=".ui.z.about.About" android:configChanges="uiMode" />

        <activity android:name=".ui.d.Favourites" android:configChanges="uiMode" />

        <activity android:name=".ui.f.Schedule" android:configChanges="uiMode" />

        <activity android:name=".ui.h.Current" android:configChanges="uiMode" />

        <activity android:name=".ui.e.Recent" android:configChanges="uiMode" />

        <activity android:name=".ui.c.Search" android:configChanges="uiMode" />

        <activity android:name=".ui.i.Show" android:configChanges="uiMode" />

        <activity android:name=".ui.g.All" android:configChanges="uiMode" />

    </application>

</manifest>